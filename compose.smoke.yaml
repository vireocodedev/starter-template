services:
  app:
    environment:
      SPRING_PROFILES_ACTIVE: prod,deployment-smoke
      VIREO_DEPLOYMENT_SMOKE_USERNAME: ${VIREO_DEPLOYMENT_SMOKE_USERNAME:?Set VIREO_DEPLOYMENT_SMOKE_USERNAME}
      VIREO_DEPLOYMENT_SMOKE_PASSWORD: ${VIREO_DEPLOYMENT_SMOKE_PASSWORD:?Set VIREO_DEPLOYMENT_SMOKE_PASSWORD}
