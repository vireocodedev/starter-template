# Relative /api is recommended: Vite proxies it in development and the canonical
# frontend container proxies it to Spring Boot in production-like deployments.
VITE_API_BASE_URL=/api
VITE_API_MODE=http
VITE_APP_NAME=Vireo Starter
